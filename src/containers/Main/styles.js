const styles = theme => ({
    root: {
        alignItems: 'center',
        display: 'flex',
        height: '100%',
        justifyContent: 'center',
        width: '100%',
        flexDirection: 'column'
    },
    paper: {
    }
});

export default styles;